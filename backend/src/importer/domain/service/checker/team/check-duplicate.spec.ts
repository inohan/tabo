import {
  checkDuplicatesWithSameMatch,
  checkDuplicatesWithSameReference,
} from './check-duplicate';

describe(checkDuplicatesWithSameReference, () => {
  test.todo('Duplicates with same reference are detected');
});

describe(checkDuplicatesWithSameMatch, () => {
  test.todo('Duplicates with same matches are detected');
});

describe('checkDuplicatesWithSameSpeaker', () => {
  test.todo('Duplicates with same speakers are detected');
});
